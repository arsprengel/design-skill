Use the design-system skill to help the user create or redesign a UI.

If $ARGUMENTS is empty, start with the Discovery Quiz (Phase 1).
If $ARGUMENTS contains a description (e.g., "/design landing page pro Rebounce"), skip Q1 (infer product type) but still ask the remaining quiz questions.
If $ARGUMENTS contains a design system name (e.g., "/design stripe"), skip the quiz entirely and go straight to Phase 3 with that design system.
